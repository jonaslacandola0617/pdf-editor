name: Fix Structure QA

on:
  push:
    branches:
      - feature/complete-structure-tools
    paths:
      - scripts/fix-structure-qa.mjs
      - .github/workflows/fix-structure-qa.yml

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[structure-qa-fixed]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/complete-structure-tools
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-structure-qa.mjs
      - run: |
          npm install
          npm run build
      - name: Commit verified fixes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "PDF Forge QA Bot"
          git config user.email "actions@users.noreply.github.com"
          git add src
          git commit -m "Fix form widget compatibility and tool labels [structure-qa-fixed]"
          git push origin HEAD:feature/complete-structure-tools
